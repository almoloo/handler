# Current Feature

Nothing in progress. Run `/feature`, `/fix`, or `/rollback` to start the next
one.

---

## History

- **Ledger Key Ring CLI pivot: decrypt-at-boot entrypoint for Riley's secrets** — backend-roadmap §4.2's mechanism: `apps/api/docker/entrypoint.sh` optionally decrypts a Key Ring ciphertext file (`AGENT_SECRETS_ENC`, `WALLET_PASS` from a secret store) into the process env before `node dist/main.js`; falls straight through to today's plaintext-env behavior if unset, so it's safe to land without breaking the current Coolify deployment. `apps/api/Dockerfile` installs `@ledgerhq/wallet-cli@2.1.0` and wires the entrypoint in. `agents.config.ts` and Riley's signing (still local viem) are unchanged — Key Ring is encryption-at-rest, not a signer. Added `docs/ledger-key-ring-setup.md` (the manual, device-required steps: enroll a machine, encrypt the two secrets, wire Coolify, the still-open "VPS with no USB port" enrollment question) and `docs/ledger-feedback.md` (DX-feedback deliverable skeleton). **Explicitly not done here** (requires a physical Ledger device and the real secret values, which I don't have access to): running `ring init`/`ring encrypt` for real, or removing the plaintext vars from `docker-compose.prod.yml` — that's the user's manual follow-up per the runbook (Completed — mechanism only, rollout is a manual follow-up)
- **Coolify Docker Compose deployment** — production Dockerfiles for api/web plus a docker-compose.prod.yml so the whole stack deploys as one Coolify resource (Completed)
- **Minimal Prisma schema + GET /health endpoint for apps/api** — fix: unblocks the paused Coolify deployment feature, which needs something real to migrate and health-check inside the api container (Completed)
- **Fix apps/api Dockerfile build order: prisma generate before nest build** — fix: the actual Coolify deployment failed because nest build ran before prisma generate, invisible in local testing due to stale local generated files (Completed)
- **Strip create-next-app / Nest CLI boilerplate from apps/web and apps/api** — fix: remove stock scaffold content (default page, logos, Hello World controller, framework READMEs) so the project reads as real app code (Completed)
- **Full Prisma database schema for apps/api** — the complete, final Postgres schema (wallets, agents, policies, activity, approvals, intents, tokens, prices, indexer cursor, demo tooling) so the schema is never touched again during the hackathon (Completed)
- **Design system tokens & theme foundation (4a)** — port the Handler Design System's tokens into apps/web ahead of implementing its 27 components with HeadlessUI across sub-features 4b–4g (Completed)
- **Root layout: providers + viewport + block-color correction** — finish app/layout.tsx (wagmi + TanStack Query provider tree, viewport meta) and fix a blocked-status color conflict between 4a's token port and the roadmap's design direction (Completed)
- **4b. Core components — Avatar, Badge, Button, IconButton** — port these four primitives from the Handler Design System canvas into components/ui/, reverting the prior feature's block-color swap to match the canvas's actual (and deliberate) slate-blocked/red-error semantics (Completed)
- **4c. Forms components — Checkbox, Input, Radio, SearchInput, Select, Slider, Switch** — port these 7 primitives from the canvas into components/ui/, using the canvas's native-control implementations rather than HeadlessUI; also fixed a Tailwind arbitrary-value bug (`text-[var(--text-*)]` silently resolving to `color` instead of `font-size`) found during audit, across this feature's 7 files and the already-merged 4b's Badge/Button (Completed)
- **4d. Feedback components — Banner, EmptyState, ProgressBar, Toast, Tooltip** — port these 5 primitives from the canvas into components/ui/, using the canvas's plain Tooltip implementation rather than HeadlessUI, applying the text-[length:...] fix from the start; audit also caught and fixed missing `type="button"` on Banner/Toast's dismiss buttons (Completed)
- **4e. Navigation components — FilterChips, NavItem, Stepper, Tabs** — port these 4 primitives from the canvas into components/ui/, fixing a hardcoded #fff in Stepper to var(--gray-0) and applying type="button" proactively; caught and fixed a border-none/border-b-2 conflict in Tabs during implementation (Completed)
- **4f. Overlay components — Dialog, Menu** — port Menu as a straight canvas port; build Dialog on @headlessui/react's real Dialog primitive instead (focus trap, portal, Escape, ARIA) since it's the Ledger co-sign confirmation screen, not a cosmetic primitive; audit verified the HeadlessUI claims directly against its shipped source (Completed)
- **4g. Data & Trust components — Card, ListRow, StatCard, Table, TrustIndicator** — port the final 5 primitives from the canvas into components/ui/, generalizing a Tailwind default-value collision rule (rounded-*/tracking-* etc. must use [var(--x)], never the bare utility) found via Table's tracking-wider; completes the 4a–4g design-system rollout. Note: Table's div-based markup (matching the canvas) lacks semantic `<table>` structure — same inherited, deferred-to-day-8 gap class as Menu/NavItem/Tooltip, despite this feature having no clickable elements otherwise (Completed)
- **Marketing landing page at `/`** — build the public marketing homepage from the imported `Handler Landing Page.dc.html` canvas, re-skinned onto the current design-system tokens; moves the future in-app Payroll screen from `/` to `/app` per confirmed scope decision (Completed)
- **HandlerWallet core contract** — the guarded agent wallet: `AgentPolicy` management, the `execute()`/`tryExecute()` 7-step check pipeline, a minimal propose/approve/deny cosign queue, and every event/custom error the backend indexer needs, per contracts-roadmap §2.1's day-2 exit criterion; `TrustReader`/`PriceConverter` land as owner-settable stubs behind stable interfaces, with real ERC-8004/Chainlink reads deferred to later features. Audit + fixes: rejected `address(0)` as a call target (a loose FLAGGED trust floor would otherwise let an agent burn funds to it), deduplicated the epoch-roll math shared by `execute()`/`tryExecute()` and `approve()`, fixed `nonReentrant` modifier ordering, added an explicit overflow guard on `PriceConverter`'s USD-8 cast, and strengthened two event-emission tests that weren't actually checking their payload (Completed)
- **Backend indexer for HandlerWallet events** — apps/api's chain/ and indexer/ modules: a transaction-wrapped, 3s cron indexer that walks HandlerWallet's logs off a local anvil deployment and decodes all 8 events into the Postgres schema (Wallet/Agent/Policy/ActivityEvent/PendingApproval/IndexerCursor), idempotently, per backend-roadmap §4.1's day-2 exit criterion. Included a minimal Foundry dev-deploy script (packages/contracts/script/DeployDev.s.sol) since no deployment existed yet, and a packages/contracts package.json `exports` map fix (the previous entry silently broke subpath imports like `addresses.ts`). Policy mirror refreshes always read chain state through rather than replaying epoch-roll math in TypeScript. Audit + fixes: added a re-entrancy guard on the cron tick (`@Interval` doesn't wait for the previous run, so an overlapping tick could otherwise race on the same cursor/agent rows), and taught the `Proposed` handler to distinguish a proposed swap (known router + calldata) from a payment, matching the classification `Executed` already did (Completed)
- **Audit fixes: indexer correctness/structure, chain-env validation, prod compose, cleanup** — fix: address all findings from the 2026-09-07 full-project audit (approve() swap-kind mislabeling, double feed rows on approve, oversized handleLog(), unvalidated chain address, missing prod CHAIN_RPC_URL, stale frozenAt, wrong fallback name, /app placeholder tokens, duplicated Avatar/ListRow initials, disabled any-lint, stray files, unpinned solc). Follow-up audit of the fix itself caught one self-inflicted regression: an earlier verification step's `git checkout` had silently reverted `generated.ts` to its pre-fix (stale) ABI after Step 1 — re-ran codegen and confirmed the `PendingApproval.kind` field is present before closing out (Completed)
- **SIWE session auth for apps/api (`auth` module)** — SIWE nonce/verify + signed HTTP-only session cookie + reusable `SessionAuthGuard`, per backend-roadmap §3/§4/§7 day 3. Guard isn't yet attached to any write route (none exist yet — `agents`/`policies`/`demo` land later); each of those features adds `@UseGuards(SessionAuthGuard)` when built. Audit + fixes: stopped a Zod validation-error leak into API responses, added the missing `Express.Request.address`/`sessionId` type augmentation the guard's contract depended on, removed guard-logic duplication by gating `/auth/session` and `/auth/logout` with `SessionAuthGuard` itself, and made nonce consumption a single atomic `updateMany` claim instead of a check-then-update race (Completed)
- **Product integrity pass on context/ guidelines** — fix: found while adding SIWE auth (which wasn't in the original guidelines) that several roadmap passages assumed a single-user, fixture-driven demo rather than a real product. Rewrote `project-overview.md` (new "Product integrity" section), `ai-interaction.md` ("Demo Integrity" → "Product Integrity" with concrete rules), `coding-standards.md` (no-mock-data rule, testing sections for api/contracts, wallet-scoping rule), and all three roadmaps: reads now require session auth and are wallet-scoped everywhere (not just writes); `TrustSource.FixtureTrustSource`/env-flag fallback and `TrustReader.setOverride` are marked as transitional debt to be deleted once real ERC-8004 reads land; hardcoded-price and mock-router fallbacks are removed from the risk/cut rules; frontend's mock Zustand store is scoped to UI-only state; demo reset is restricted to rows owned by the showcase wallet; tests are made a required part of "done" rather than an optional smoke script (Completed)
- **Product integrity audit follow-ups (small items)** — fix: address the small, independently-fixable findings from the 2026-09-07 full-project audit against the new guidelines (the audit found no violations requiring rollback, only gaps). SIWE `verify()` now binds `domain` and rejects a mismatched `chainId` before the nonce is even claimed (new `SIWE_DOMAIN`/`SIWE_CHAIN_ID` env vars); `HandlerWallet.trustReader`/`priceConverter` marked `immutable` (no ABI change, confirmed via `pnpm codegen` no-op); `/health` no longer probes the hackathon-only `DemoRun` table, using `SELECT 1` instead; removed the root `pnpm smoke` script entry pointing at a nonexistent `scripts/smoke.ts` (and its CLAUDE.md mention) until that file actually exists; added `apps/api/test/auth.e2e-spec.ts`, the first real e2e test, covering nonce→verify→session→logout, the 401 path, and nonce-reuse rejection against the local Postgres instance, and turned off `passWithNoTests` on the e2e config now that a real spec exists. Left to their already-scheduled features per the audit: factory-based multi-wallet indexing, `TrustReader`/`PriceConverter` stub-to-real-registry swaps, and the `FIXTURE`/`isDemo` schema cleanup. Also fixed a stray literal `</content>` tag left at the end of this file by an earlier tool artifact (Completed)
- **Riley agent runtime: real 1inch swap through the policy wallet** — backend-roadmap day 3's second half: the `agents` module, Riley's own session-key `viem` signer, a real 1inch Swap API quote, and `POST /agents/:id/run` calling `HandlerWallet.tryExecute()` directly; freezes the session interface (every catalog agent signs and sends its own `tryExecute({target,data,value})`, never routed through a shared API signer). Audit + fixes: added the tx-submission/receipt-decision test coverage that was missing (success/blocked/reverted/submission-failure/receipt-timeout), an in-flight lock so a double "run now" can't submit two real swaps for the same wallet/agent, a fast 409 when the policy mirror already shows frozen (no wasted 1inch call), a typed 504 instead of an opaque 500 when the receipt wait times out (Intent stays at SUBMITTED rather than guessing FAILED), and swapped a hand-rolled address regex for viem's `isAddress` to match `chain.config.ts`'s existing pattern (Completed)
- **Fix Coolify build failure: missing `apps/web/public/`** — fix: restore `apps/web/public/` (deleted by the earlier boilerplate strip but still `COPY`'d by `apps/web/Dockerfile`) so the untracked-empty-directory breaks every Coolify deploy no longer does (Completed)
- **Fix Coolify `api` container crash-loop: missing `RILEY_SESSION_KEY`** — fix: `RILEY_SESSION_KEY` was required by `agents.config.ts` (no default, unlike other agent env vars) and documented in `.env.example`, but never wired into `docker-compose.prod.yml`'s `api` service, so the container crashed at NestJS bootstrap in production before its first healthcheck (Completed)
- **Partner-track strategy correction: Ledger primary, Chainlink stretch, drop 1inch as a track target** — audited the actual ETHOnline 2026 sponsor prize pages (1inch, Ledger, Chainlink, and 7 others) against what Handler builds. Found the 1inch track requires Aqua/SwapVM contracts (not the Swap API Riley uses), the Ledger track requires the Ledger Key Ring CLI for agent secret custody (not just the planned owner-side Device Management Kit co-sign), and Chainlink's only open track needs a CRE Confidential Workflow (plain `AggregatorV3` reads don't qualify) — none of which matched the tracks named in `CLAUDE.md`/`project-overview.md`. Updated `project-overview.md`, `CLAUDE.md`, `backend-roadmap.md`, `contracts-roadmap.md`, and `frontend-roadmap.md` to: make Ledger the primary target via a planned pivot of Riley's secrets (`RILEY_SESSION_KEY`, `ONEINCH_API_KEY`) from plaintext Coolify env to Ledger Key Ring ciphertext decrypted at container boot (§4.2/§6 of backend roadmap, not yet implemented), add a Chainlink Confidential Workflow stretch goal (backend roadmap §4.5, explicitly cuttable, attempted only after feature freeze), and reframe 1inch/plain Chainlink feeds as real product integrations with no attached prize track. Verification pass afterwards (installed `@ledgerhq/wallet-cli` 2.1.0 and read its `ring` help + Ledger's docs) corrected a wrong first draft: the Key Ring CLI is hardware-rooted *encryption at rest* (`ring init` once with device, `ring encrypt/decrypt` headless with `WALLET_PASS`), not a remote signer — Riley keeps signing locally with viem. Also recorded that the repo's first commit (2026-09-05) satisfies Ledger's "started during the event" rule, that Ledger scores DX feedback (added `docs/ledger-feedback.md` as a deliverable), and that Chainlink Confidential Workflows are in private beta (day-8 go/no-go spike) (Completed)
- **TrustReader: real ERC-8004 Identity + Reputation registry reads** — contracts-roadmap day-3 exit criterion: replaces the owner-settable `setOverride` stub with a `tierOf()` resolving VERIFIED/NEW/FLAGGED from the real ERC-8004 registries on Base; unblocks backend's day-4 `trust/` module and `scripts/register-agents.ts`. Design correction found mid-implementation: the real deployed registries have no on-chain address→agentId lookup, so `TrustReader` keeps a small permissionless `syncAgent(agentId)`-warmed cache instead of being a pure stateless proxy (contracts-roadmap.md §2.2 updated to match). Audit fix: made the VERIFIED tier threshold constants `public` so the backend `trust/` module (which must mirror them exactly) can read them on-chain instead of hand-copying magic numbers (Completed)
- **Backend trust/ module: real ERC-8004 tiers + showcase registration script** — backend-roadmap day-4 exit criterion: `TrustService` resolves `Agent.trustTier` from the real ERC-8004 registries on a 60s cron (mirroring `TrustReader.sol`'s `tierOf`/`_averageFeedback` in TypeScript, with its registries/thresholds read on-chain rather than hand-copied), keeping an agent's cached tier untouched (not falling back to FLAGGED) when the registries are unreachable. `apps/api/scripts/register-agents.ts` registers the showcase subcontractor (real feedback → VERIFIED) and a second catalog agent (no feedback → NEW) in the real registries, idempotently. Audit + fixes: a malformed feedback entry (attacker-settable `valueDecimals` > 18 on the permissionless registry) could throw and permanently freeze an agent's cached tier — now excluded from the average instead of aborting it; dropped several redundant type assertions on already-inferred `readContract` results to match `indexer.service.ts`'s pattern; documented `register-agents.ts`'s DB-only (not on-chain) idempotency check as an acceptable limitation for a manual, one-time script. **Not verified**: a live tick against `pnpm dev:chain` — this sandbox has no `BASE_RPC_URL`/agent keys in `.env`, so that's a manual follow-up before relying on this in the actual demo (Completed)
- **5a. Payroll + catalog + agent-file reads (`policies` module, `GET /agents`, `GET /agents/catalog`, `GET /agents/:id`)** — backend-roadmap day-4 REST surface, first of two sub-features; unblocks the frontend's Payroll screen/hire picker, the current critical-path gap. New `policies` module (Prisma-only, no `ChainModule` dependency per the module dependency rule) with `PoliciesService.spentTodayUsd`/`listForWallet`/`catalog`/`agentFile` and a pure `policySentences()` no-jargon formatter; wired onto `AgentsController` with `SessionAuthGuard` and wallet-scoped queries. Also moved `indexer/format.ts` → `common/format.ts` so `policies` doesn't have to depend on `indexer` for `formatUsd`. Proven end to end against real Postgres by 8 new e2e cases (401s, empty-vs-real payroll, the two-wallet non-leak case, agent-file's `policy: null` vs. hired shape, 404 for an unknown agent) — no live anvil/`.env` in this sandbox, so the literal `curl` against `pnpm dev:chain` from the roadmap's day-4 exit criterion is still a good manual sanity check before the demo. Audit + fixes: extracted a shared `toAgentFilePolicy` helper to remove an 11-field mapping duplicated between `listForWallet` and `agentFile`, and tightened `trustTier`/`trustSummary` from bare `string` to the locked `TrustTier` union. **5b (the `activity` module: `GET /activity`, `GET /events/stream` SSE) remains queued as a separate `/feature` run** (Completed)
- **5b. Activity feed + realtime (`activity` module, `GET /activity`, `GET /events/stream`)** — backend-roadmap day-4 REST surface, second (and last) of two sub-features; completes the day-4 read surface the frontend's activity feed/notification cards build against. New `activity` module (Prisma + Auth only, no `ChainModule`/`AgentsModule`, matching the read/serve-module dependency rule): `GET /activity` is a `seq`-cursor-paginated, filterable (`all`/`blocked`/`pending`) feed; `GET /events/stream` is a poll-based SSE endpoint (no queue/pubsub infra — `ACTIVITY_SSE_POLL_MS`, default 2s) that honors `Last-Event-ID` on reconnect, since `ActivityEvent.seq` is deliberately its own resume cursor per the schema's doc comment. Both routes are session-gated and wallet-scoped; a fresh sign-in with no wallet yet gets an empty feed/a silent stream rather than an error, matching `GET /agents`'s precedent. Proven end to end against real Postgres by 7 new e2e cases (401s, empty feed, filter rejection, real rows + filtering + pagination + two-wallet non-leak, malformed-query rejection, and a live SSE connect-and-receive-one-event case). Found and fixed a real, pre-existing test-infra bug while landing this: `auth.e2e-spec.ts`/`agents.e2e-spec.ts`'s `afterAll` deleted *every* `Session`/`SiweNonce` row matching `contains: '0x'` rather than just their own, which raced against other e2e spec files running concurrently against the same Postgres instance — adding this third file surfaced it as an intermittent 401; fixed all three specs to scope cleanup to each file's own tracked owner addresses, confirmed via repeated clean `test:e2e` runs. Audit + fixes: added missing unit-test coverage for `resolveStartingCursor`'s malformed-`Last-Event-ID` fallback branch and for the locked malformed-`before`/`limit` → 400 behavior (both were previously unverified, though independently confirmed correct), hoisted a duplicated Prisma `include` shape into one shared constant, and hardened the SSE e2e test's early assertions so a future failure reports a clear error instead of a 5s timeout (Completed)
