# apps/web

Handler's frontend — Next.js (App Router) + Tailwind. See the repo root
`CLAUDE.md` and `context/frontend-roadmap.md` for stack, conventions, and
build plan.

```bash
pnpm --filter web dev     # dev server
pnpm --filter web build   # production build
pnpm --filter web lint    # eslint
```
